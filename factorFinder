'''Factor Finder: finding any two numbers that multiplies into the target number'''



print('By Jing Wei Zeng')
print('Linkedin: https://www.linkedin.com/in/zengkev/')
print()
print()


import math, sys

while True:
    print('Enter a positive number or QUIT: ')
    response = input('> ')
    if response.upper() == 'QUIT':
        sys.exit()

    if not (response.isdecimal() and int(response) > 0):
        continue
    number = int(response)

    factors = []

    for i in range(1, int(math.sqrt(number)) + 1): # If a and b are the same, then they both must be the square root of n
        if number % i == 0: # if there is not reminder
            factors.append(i)
            factors.append(number // i)

    # Convert to a set to get rid of duplicaes factors:
    factors = list(set(factors))
    factors.sort()

    for i, factor in enumerate(factors):
        factors[i] = str(factor)
    print(', '.join(factors))
